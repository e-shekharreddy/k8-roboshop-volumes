# Storage class is admin task
apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: roboshop-ebs
provisioner: ebs.csi.aws.com
volumeBindingMode: WaitForFirstConsumer # "kubectl get sc"
reclaimPolicy: Retain # "kubectl get sc rboshop-ebs -o yaml" & "kubectl api-resources | grep sc"
